---
- name: "Configure PC"
  hosts:
    - "jorenLaptop"
    - "jorenPC"
  become: no
  vars_files:
    - "vars/{{ lookup('env', 'VAULT') | default('vault', true) }}.yml"
  roles:
    - "packages"
    - "user"
    - "ssh"
    - "zsh"
    - "i3"
    - "polybar"
    - "dunst"
    - "xorg"
