---

- name: "Install packages"
  become: yes
  package:
    name:
      - "docker"
      - "docker-compose"
      - "python-docker"

- name: "Add user to docker group"
  become: yes
  user:
    name: "{{ ansible_user }}"
    groups: "docker"
    append: yes
