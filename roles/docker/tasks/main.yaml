---

- name: "Install packages"
  become: true
  package:
    name:
      - "docker"
      - "docker-compose"
      - "python-docker"

- name: "Add user to docker group"
  become: true
  user:
    name: "{{ ansible_user }}"
    groups: "docker"
    append: true
