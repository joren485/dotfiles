---
- name: "Configure PC"
  hosts:
    - "lilo5"
  become: no
  vars_files:
    - "vars/{{ lookup('env', 'VAULT') | default('vault', true) }}.yml"
  roles:
    - role: "user"
      tags: "user"

    - role: "zsh"
      tags: "zsh"

    - role: "ssh"
      tags: "ssh"
